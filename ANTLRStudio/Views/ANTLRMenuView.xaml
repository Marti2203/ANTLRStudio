<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:ANTLRStudio.ViewModels;assembly=ANTLRStudio"
             xmlns:vs="clr-namespace:ANTLRStudio.Views;assembly=ANTLRStudio"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             Name="Menu"
             x:Class="ANTLRStudio.Views.ANTLRMenuView">
    <Design.DataContext>
        <vm:ANTLRMenuViewModel/>
    </Design.DataContext>
    <Menu DockPanel.Dock="Top">
        <MenuItem Header="_File">
            <MenuItem Header="_Open Grammar" Command="{Binding MenuViewModel.OpenGrammar}" CommandParameter="{Binding #Menu}">
            </MenuItem>
            <MenuItem Header="_Close Grammar" Command="{Binding MenuViewModel.CloseGrammar}" CommandParameter="{Binding #Menu}"/>
            <Separator/>
            <MenuItem Header="_Exit" Command="{Binding MenuViewModel.Exit}"/>
        </MenuItem>
    </Menu>
</UserControl>
